namespace MauiTheme;
public static class BuilderExtensions
{
    //public static MauiAppBuilder UseMauiTheme<TApp>(this MauiAppBuilder builder, Action<ThemeConfiguration> themeConfiguration) where TApp : Application
    //{
    //    var theme = new ThemeConfiguration();
    //    themeConfiguration.Invoke(theme);

    //    Theme.Initialize(theme.Resources, theme.DefaultTheme, theme.DefaultResource, typeof(TApp).Assembly);

    //    return builder;
    //}
}
